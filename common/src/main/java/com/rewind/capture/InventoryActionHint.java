package com.rewind.capture;

/**
 * Loader-provided clue about the vanilla interaction that just happened.
 */
public enum InventoryActionHint {
    UNKNOWN,
    MOVE,
    SWAP,
    SHIFT_CLICK,
    HOTBAR_SWAP,
    DROP,
    PICKUP,
    SORT
}
